import { constructMetaData } from "@/lib/metadata";


export const metadata = constructMetaData({
    title: "Analytics | Vity",
    description: "This is the Analytics for Vity",
});


export default function Analytics() {
    return (
        <>
            <h1>Analytics</h1>
        </>
    );
}

