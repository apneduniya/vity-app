import { constructMetaData } from "@/lib/metadata";


export const metadata = constructMetaData({
    title: "Integrations | Vity",
    description: "This is the Integrations for Vity",
});


export default function Integrations() {
    return (
        <>
            <h1>Integration</h1>
        </>
    );
}

